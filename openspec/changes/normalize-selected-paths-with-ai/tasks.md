## 1. Cloudflare 与依赖配置

- [x] 1.1 使用 Cloudflare 原生 Workers AI 绑定并保留现有 `wrangler` 开发依赖；移除不再需要的 `ai` 和 `workers-ai-provider` 运行时依赖
- [x] 1.2 在 `wrangler.jsonc` 中保留 `AI` 绑定，新增 `AI_ACTOR_RATE_LIMITER`（20 次/60 秒）和 `AI_COLO_RATE_LIMITER`（120 次/60 秒），为两者分配账户内唯一 namespace ID，且不改变 TanStack Start Worker 入口
- [x] 1.3 运行 `wrangler types` 生成绑定类型，使服务端识别 `AI` 和两个 Rate Limiting 绑定，同时确认这些类型和 `cloudflare:workers` 导入不进入客户端包
- [x] 1.4 固定模型常量 `@cf/mistralai/mistral-small-3.1-24b-instruct`、提示版本 `shape-normalization-v6-rounded-contours`、服务端超时 20 秒、客户端超时 25 秒和常用符号矢量阶段输出上限 768 Token
- [x] 1.5 记录认证远程/预览测试方法、Workers AI 用量与费用、Rate Limiting 位置局部/最终一致语义、namespace ID 冲突处理和现有 `main` 分支部署流程

## 2. 共享契约与选区预处理

- [x] 2.1 定义严格 Zod Schema 和 TypeScript 可辨识联合类型，覆盖请求、`known-shape`、`common-symbol`、`unsupported`、公开失败结果及所有数值上限，并对对象使用 strict 模式拒绝未知字段
- [x] 2.2 实现选区资格检查：仅允许 1–8 条颜色和粗细完全一致的笔画，不允许便签，至少 3 个不同点，且屏幕边界至少一边达到 12 CSS px
- [x] 2.3 按文档层级顺序规范化序列化选中元素的 ID、类型、`createdAt`、样式和全部点（含压力），使用 Web Crypto SHA-256 生成仅客户端保存的源指纹
- [x] 2.4 将每条笔画转换为选区局部单位坐标，保留笔画边界和顺序，移除世界位置、压力、样式、ID、时间戳及无关文档数据，并校验所有数值有限
- [x] 2.5 使用按弧长重采样和确定性 Ramer–Douglas–Peucker 简化，保留闭合关系、角点和极值；从单位容差 `0.005` 开始按需提高，直到总点数不超过 512
- [x] 2.6 仅用选中路径生成 256×256 PNG：白色背景、黑色 6px 圆角线条、16px 留白、保持宽高比、不含文字/样式/元数据，并拒绝超过 64 KiB 的编码结果
- [x] 2.7 添加资格、严格 Schema、指纹稳定性/变化检测、归一化、按弧长重采样、简化、预览像素尺寸/内容/大小、隐私字段移除和异常数值测试

## 3. 本地几何分析与确定性重建

- [x] 3.1 定义类型化图形策略注册表和固定门槛常量：已知图形置信度 `0.80`、常用符号 `0.85`、符号 Chamfer RMS `0.15` 和 Hausdorff `0.35`；已知图形分类不再经过本地轮廓否决门
- [x] 3.2 实现单笔画闭合检测和多笔画端点图拼接，允许在对角线 8% 内重排/反转端点，并对简单闭合图形拒绝分叉拓扑
- [x] 3.3 实现通用按弧长重采样、主轴分析、稳定角点提取、双向 RMS/Chamfer/Hausdorff 距离和轴对齐视觉尺寸变化计算
- [x] 3.4 实现圆形和椭圆策略，包括近圆门槛、平均拟合直径、旋转椭圆、闭合检查和圆形到椭圆的确定性回退
- [x] 3.5 实现正方形和长方形策略，包括近方形门槛、平均边长、相邻边垂直、对边平行和正方形到长方形的确定性回退
- [x] 3.6 实现三角形和平行四边形策略，分别保持三个拟合角点，或保持中心、方向、宽高和倾斜量并强制两组对边平行
- [x] 3.7 实现正五边形和正六边形的确定性模板重建，并映射到源选区视觉边界
- [x] 3.8 实现正五角星模板重建，使用 `1 : 0.381966` 内外半径比例和十个交替顶点
- [x] 3.9 实现常用符号 DSL 语法/复杂度校验：1–64 字符小写 slug、最多 8 条路径/32 个总段/512 个采样点、每路径唯一首段 `move`、端点 `[0,1]`、控制点 `[-0.25,1.25]`
- [x] 3.10 实现常用符号模板可见边界重新归一化、源轮廓相似度门控、轴对齐边界映射和每曲线最多 16 点的自适应采样，不对模板额外旋转
- [x] 3.11 实现版本 1 兼容 `StrokeElement` 构造，保持原颜色和粗细，为每条输出生成唯一 ID，并在一次提交中共享同一 `createdAt`
- [x] 3.12 为所有已知图形、旋转/多笔画图形、五角星、有效和恶意 DSL、多路径符号、错误 AI 标签、所有阈值边界、退化路径、样式保持和点数上限添加夹具测试

## 4. 仅服务端 Mistral 两阶段推理服务

- [x] 4.1 创建共享纯数据 Schema 模块和 `.server.ts` 推理适配器；仅服务端适配器可从 `cloudflare:workers` 读取 `env.AI` 与 Rate Limiting 绑定
- [x] 4.2 直接使用原生 `env.AI.run` 和模型原生 `guided_json` 接入 Mistral Small 3.1；显式发送简化点和 data URL 预览，并对返回值执行严格 Zod 校验
- [x] 4.3 编写 `shape-normalization-v6-rounded-contours` 提示：第一阶段使用扁平 Schema 分类，明确区分圆润轮廓、斜四边形与矩形，只有 `common-symbol` 才执行第二阶段受限矢量 DSL 生成，不确定时返回不可识别
- [x] 4.4 使用非流式响应、20 秒服务端截止时间、常用符号阶段最多 768 Token，禁止浏览器和服务端自动重试或静默切换模型
- [x] 4.5 在推理前重复执行严格请求校验和 PNG 类型/尺寸/字节数检查；无效输入不得消耗应用限流令牌或调用 AI
- [x] 4.6 在校验后依次执行 `AI_ACTOR_RATE_LIMITER` 和 `AI_COLO_RATE_LIMITER`；actor key 使用 `cf-connecting-ip` 与操作名，缺失时使用 `unknown`，任一失败均返回 `rate-limited`
- [x] 4.7 将绑定不可用、应用/平台限流、超时、原生 AI 调用、结构化输出、模板、无法推断和低置信度情况映射为不抛异常的公开类型化结果
- [x] 4.8 记录请求 ID、模型/提示版本、笔画/点数量、延迟、结果类别、置信度分桶和错误类别；禁止记录来源地址、点、PNG、提示、模型原始响应、白板坐标或文档数据
- [x] 4.9 使用模拟原生 AI 与 Rate Limiting 绑定测试全部结果分支、调用顺序、无重试、超时、缺失绑定、置信度缺省、类 JSON 分类恢复、常用符号二次调用和日志脱敏

## 5. 类型化 TanStack Start 服务端边界

- [x] 5.1 实现 POST `createServerFn`，使用输入校验器调用仅服务端适配器，并复用 `src/start.ts` 现有 CSRF 中间件
- [x] 5.2 确保服务端函数只返回公开可辨识结果，不暴露原生 AI 错误、提示、模型原始输出、堆栈、来源地址、绑定或凭据
- [x] 5.3 添加服务端函数测试，证明无效载荷在推理前拒绝、限流结果不调用 AI、有效多模态载荷只返回公开描述符/模板、错误结果可序列化
- [x] 5.4 审计生产客户端包，确认 `cloudflare:workers`、绑定访问、模型/提示、`guided_json`、data URL 组装和服务端实现均不存在

## 6. 白板交互与原子文档替换

- [x] 6.1 将选框右侧顶部按钮原本的控制台输出替换为标准化流程，并只对符合条件的笔画选区显示；不改变首页 `ssr: true` 和完整文档 SSR 模型
- [x] 6.2 添加请求状态、`aria-busy`、重复点击阻止、25 秒 `AbortController`、单调请求序号和迟到响应忽略，同时保持白板其他工具可用
- [x] 6.3 实现无障碍 Toast 区域（`role="status"`、`aria-live="polite"`），覆盖成功、无法识别、选区变化、超时、限流、服务不可用和一般失败文案
- [x] 6.4 请求前捕获文档顺序中的源 ID、SHA-256 指纹、样式、轴对齐边界和最早层级索引；请求期间允许继续编辑白板
- [x] 6.5 响应后重新计算源指纹并检查所有源元素存在；移动、擦除、清空、撤销、重做或任何点/样式/顺序变化均丢弃结果并显示选区变化 Toast
- [x] 6.6 对已知图形执行确定性重建而不增加本地分类否决，对常用符号执行 DSL/相似度安全门控，并在全部门槛通过后构造一条或多条替换笔画
- [x] 6.7 从最早源层级连续插入替换笔画，保持所有无关元素的相对顺序，通过现有 `commitDocument` 提交一条“标准化图形”历史记录并选中全部新笔画
- [x] 6.8 确保撤销恢复所有原始元素，重做从 after 快照恢复完全相同的 ID、`createdAt`、点、样式和层级，且不再次调用 AI
- [x] 6.9 对无法推断、低置信度、几何/相似度/模板无效、超时、限流、不可用、失败、取消或过期结果保持原始元素、选区和历史记录不变

## 7. 验证、隐私与发布

- [ ] 7.1 添加组件测试，覆盖按钮资格与位置、PNG 生成、处理中、重复点击、Toast、单/多路径成功替换、取消/迟到响应和每条非破坏性失败路径
- [x] 7.2 添加历史、持久化和 PNG 导出测试，证明标准化结果可撤销/重做、300ms 自动保存后可重载、无需文档迁移且导出不含选区控件
- [x] 7.3 建立固定验收语料：每个已知图形至少 5 个鼠标/触控笔样本、至少 20 个未枚举常用符号、20 个不支持/无法推断样本及异常载荷/模板
- [x] 7.4 使用真实 Mistral Small 3.1 执行认证远程浏览器冒烟测试，验证点+PNG 多模态输入、原生 `guided_json`、两阶段 Schema、20 秒截止时间、无重试和日志脱敏
- [ ] 7.5 计算发布指标：已知图形正确替换率至少 90%，常用符号至少 80%，不支持样本错误替换率不超过 5%，异常与过期结果 100% 保持原图不变；未达标时不得合并或部署到 `main`
- [x] 7.6 运行 Prettier、ESLint、TypeScript、全部单元/组件测试、Cloudflare 生产构建、Wrangler dry run 和远程预览冒烟测试
- [x] 7.7 检查生产客户端包和 Worker 日志，确认绑定/提示仅服务端存在，且日志不含点、选区 PNG、来源地址、模型原始响应或文档数据
- [ ] 7.8 验证部署后的桌面端和移动端流程，包括按钮位置、Toast、视觉尺寸/方向/粗细保持、多路径选择、撤销/重做、重新加载和页面无新增溢出
